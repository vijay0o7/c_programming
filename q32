#include<stdio.h>
int main()

{
	int a,b,sum;
	printf("Enter a Number  :");
	scanf("%d",&a);
	printf("Enter Second Number  :");
	scanf("%d",&b);
	sum=a+b;
	printf("the addition is  :%d",sum);
	return 0;
}
